import { Filters } from './Filters'

export function Header () {
    return (
        <header>
            <h1>Header useContext, useId, useReducer</h1>
            <Filters/>
        </header>
    )
}